node_modules/
*.log

# Credenciais. O .env guarda token do bot e, se voce ligou a Web API, o refresh
# token do Spotify — que da acesso de leitura ao seu historico de escuta.
.env
.env.*
!.env.example

# binario do yt-dlp, baixado por: npm run setup:ytdlp
bin/

# Matches spotify->youtube. Reconstruivel, mas NAO publique: as URLs assinadas
# do googlevideo carregam o seu IP publico no parametro "ip", e as chaves sao um
# retrato do que voce andou ouvindo.
cache/
